# План повышения качества regen_csv/fb2_compiler

Согласован 2026-09-01. Контекст: эвристический 6-пасовый пайплайн
определения автора/серии/жанра — самое слабое место проекта (см.
`tests/integration/fb2parser_core/README.md` про регрессионные фикстуры,
пункт 1 ниже).

## 1. Регрессионный fixture-набор — ✅ Готово

`tests/integration/fb2parser_core/` + `tests/data/regen_library/` +
`scripts/build_regen_fixtures.py`. Процесс пополнения — в README.md там.

## 2. Убрать недетерминизм многопроцессности — ✅ Готово

`Pass1ReadFiles.execute()` собирал `records` в порядке ЗАВЕРШЕНИЯ
воркеров (`concurrent.futures.as_completed`), а не в стабильном
порядке — из-за чего последующий консенсус (порядок слов в имени
автора, дробные номера подсерий типа "2.1"/"2.2", капитализация серий)
иногда давал разные результаты между прогонами без изменения кода.

Фикс: `records.sort(key=lambda r: str(r.file_path))` сразу после сбора
результатов в `pass1_read_files.py`. Проверено на полной библиотеке
Test1 (~12000 файлов): два подряд прогона одинакового кода дали
**0 расхождений** (было — десятки на каждую пару прогонов). Diff
против baseline (без фикса) — 172 записи, все относятся к уже
известным категориям недетерминизма (имя автора, капитализация серии,
перестановка дробных номеров), теперь зафиксированы в одно конкретное
значение; данных не потеряно.

## 3. Задокументировать приоритет источников — ✅ Готово

`docs/series-priority.md` — единый источник правды: иерархия
folder_dataset > folder_hierarchy > filename_prefix > filename_named_arc
> metadata, правило коллизий номеров, ссылки на код и тесты. Указатель
добавлен в docstring `regen_csv.py`.

## 4. Ревизия конфиг-патчей (`author_surname_conversions` и т.п.) — ✅ Готово

Выполнено на реальной библиотеке пользователя, доступной с этой машины
двумя путями сразу: по SSH (`/mnt/media/Docs/Books` — 50163 fb2, 107 ГБ;
`/mnt/media/Docs/EBookLibrary` — 660 fb2, 2 ГБ) и через сетевую шару
(`\\turnkey\Docs\Books`, см. локальный dev-сервер).

**Как проверялось:** для всех 61 записи `author_surname_conversions`:
1. Точное совпадение ключа с именем папки — 17/61 совпали.
2. Для остальных — присутствие каждого значимого слова ключа в "шапке"
   FB2-файлов (первые 16 КБ — метаданные автора всегда там, до тела книги
   и base64-обложек; полнотекстовый grep по 107 ГБ был бы неоправданно
   долгим). 43/44 подтвердились.

**Результат:** только одна запись оказалась мёртвой —
`"Постельгио": "Постегильо"` — ни одного совпадения ни по одному слову
нигде в текущей библиотеке. Удалена из `app_settings.json`. Остальные 60
записей — реально применяются к существующим файлам, не трогали.

Другие точечные списки в `app_settings.json` (`series_conversions` — 1
запись, `special_series_values` — 1 запись) слишком малы, чтобы
накопить "устаревшее"; `series_folder_blacklist` и подобные — общие
правила, а не патчи под конкретные книги, вне области этого пункта.

## 5. Анализ полной библиотеки на аномалии парсинга — 🔄 продолжается, передано на другую машину

**Зачем.** ИИ-агента нельзя поселить на сервер (старый 2-ядерный AMD E-350
не тянет современный Node.js/Claude Code), а вручную разобрать логику
regen_csv/fb2_compiler на 50163 файлах нереально. План: прогнать
`regen_csv`/`find_groups` разом по ВСЕЙ библиотеке (не на сервере — на
машине с бóльшим числом ядер), получить CSV, и уже по CSV эвристиками
находить подозрительные случаи для точечного разбора — тем же способом,
каким были найдены и закрыты кейсы Bykov/Panov/Аржанов ранее.

**Сделано (2026-09-01/02, на Windows-машине через `\\turnkey\Docs\Books`):**
- `regen_csv` по всей библиотеке: **49519 записей**, 114.8 мин.
- `find_groups`: **6651 групп** для компиляции, 80.25 мин (после фикса
  ниже; до фикса процесс казался зависшим — CPU был около нуля).
- Побочно найден и исправлен реальный прод-баг (закоммичено, `eaf3341`):
  `_extract_year_from_fb2`, `_extract_date_from_fb2`,
  `_precompiled_range_from_content` (`fb2_compiler.py`) и хелпер в
  `precache.py` делали `path.read_bytes()` (весь файл) и потом отрезали
  первые N КБ — на сетевой шаре это гоняло по сети мегабайтные файлы
  (обложки) ради чтения только заголовка. Исправлено на частичное чтение
  (`f.read(N)`). Баг актуален и для прод-компиляции на сервере, не только
  для анализа.

**Результаты анализа (НЕ в git — `.gitignore`'нуты, переносятся вручную
между машинами; на момент передачи уже скопированы на вторую станцию):**
- `src/fb2_data/csv/full_library_records.csv` — все 49519 записей после
  regen_csv (author/series/genre и т.д. по каждому файлу).
- `src/fb2_data/csv/full_library_groups.csv` — 6651 групп компиляции
  (author, series, n_books, volume_range, series_complete,
  order_determined, alphabetical_order, part_count, file_paths).
- Из records: **17929 записей** имеют `proposed_series`, но НЕ попали ни
  в одну группу — это не обязательно баг (могут быть реальные синглы),
  но именно тут стоит искать пропущенные дубликаты/расхождения в
  написании автора или серии.

**Дальше (не начато) — предложенные эвристики для сужения списка:**
1. Группы с `order_determined=False` и/или `alphabetical_order=True` —
   слабая уверенность в порядке томов, компилятор просто разложил по
   алфавиту вместо реального номера.
2. Аномальные `volume_range`/`part_count` на автора (например один автор
   с кучей мелких групп по 2 книги вместо одной большой серии —
   подозрение на несведённые варианты написания серии).
3. 17929 ungrouped-with-series (см. выше) — отфильтровать по частоте
   автора/серии, чтобы найти те, где явно "распалась" одна серия на
   несколько написаний.
4. Записи со слабым `author_source`/`series_source` (например
   `filename`-only без подтверждения метаданными) на фоне большого
   объёма книг этого автора в других папках.

Дальше — как обычно: по каждому найденному кейсу разобраться в первопричине,
исправить, проверить на реальных данных, закрепить регрессионным тестом в
`tests/integration/fb2parser_core/` (см. README.md там), задеплоить.

**Найдено и исправлено (2026-09-02, применяя эвристику 3 и точечный разбор
`ungrouped-with-series` — реальные fb2-файлы недоступны с этой машины,
проверено на синтетических BookRecord, см.
`tests/integration/fb2parser_core/test_author_series_consensus.py`):**

- **Баг №1 (некорректный):** `series_processor.apply_author_consensus()`
  требовал "≥50% high-priority записей за автора-консенсус", но при
  ОДНОМ high-priority файле в папке 100% от одного голоса тривиально
  проходили порог. Папка-антология "Серия - «Скандинавская линия»\
  Скандинавская линия «НордБук»" (45 файлов, 33 разных реальных
  metadata-автора) содержала ровно 1 файл с filename-уровня сигналом
  (книга самого Сьона) — и её автор навязывался всем остальным 44
  файлам с другими настоящими авторами (Альвтеген, Биргиссон, Буман и
  т.д.). Исправлено: требуем ≥2 high-priority голосов, иначе не
  доверяем консенсусу. Легитимный 2+-голосный консенсус продолжает
  работать (проверено отдельным тестом).
- **Баг №2 (косметический, узкий):** `_postcheck_series_folder_blacklist()`
  обрезал префикс `"Серия - «"`, но не парную закрывающую `»` — серия
  оставалась вида `"Колычев. Лучшая криминальная драма»"` с висячей
  кавычкой. Единственный такой случай во всей 49519-записевой
  библиотеке (подтверждено полным grep перед фиксом). Исправлено:
  обрезаем и закрывающую кавычку, когда она есть; вложенные
  самостоятельные пары кавычек (`«Приключения «Икс»»`) не трогаем.

**Баг №3 — ✅ подтверждён и починен на реальных данных (см. live-раздел
ниже).** Исходно: `series_processor.apply_series_consensus()`
и дублирующая логика в `pass4_consensus.py` (строки ~1093-1096) читают
`record.extracted_series_candidate` — поле, которое по своему docstring
в `pass1_read_files.py` должно быть "серия из имени файла" и никогда не
содержать путь папки. В реальной CSV найдена запись, где оно всё же
содержало путь: у файла `...\Данильченко-Олег\Имперский вояж\1. Из
варяг в небо.fb2` `extracted_series_candidate` = `"Данильченко-
Олег\Имперский вояж"` (с именем автора-папки внутри), из-за чего два
СОВСЕМ ДРУГИХ файла той же серии в других папках (сборники "Данильченко.
Цикл «Имперский вояж».fb2" и "...Тропинка к Млечному пути.fb2")
получили `proposed_series = "Данильченко-Олег\Имперский вояж"` вместо
чистого "Имперский вояж" (`series_source=author-consensus`) — то есть
одна серия физически рвётся на 2 группы при компиляции (тот же класс
бага, что чинили для Эльтеррус/Отзвуки серебряного ветра ранее).

Фикс: в `apply_series_consensus()` берём только хвост
`extracted_series_candidate` после последнего `\` перед использованием
как ключа консенсуса — поле в принципе не должно содержать путь, так
что это правка всегда безопасна независимо от точного места её
возникновения.

**Не доведено до конца:** я НЕ смог достоверно воспроизвести исходный
триггер бага на доступных мне копиях библиотеки (`Test1`/`Test2`) —
там подмножество файлов Данильченко ("Современный фантастический
боевик(по авторам и циклам)\Данильченко Олег\..." + "Серия - «Боевая
фантастика. Коллекция»\Данильченко Олег - Имперский вояж...fb2") уже
резолвится КОРРЕКТНО что с фиксом, что без него — значит, реальный
баг триггерят папки, которых нет в этих копиях: `СЕРИЯ.LitRPG\
Hеофициальная серия книг (разные издательства)\Данильченко-Олег\
Имперский вояж\` и/или `Серия - «Миры Содружества (Вселенная
EVE-online)»\Данильченко Олег\`.

**Обновление той же сессии — масштаб подтверждён, паттерн общий, не
только Данильченко.** Тот же класс бага найден ещё в 88 записях
(~30 авторов) по всей библиотеке через
`series_source == "author-consensus" and "\\" in proposed_series`:
- **Поселягин Владимир\Криминал** (11 записей) — точно та же папка
  `СЕРИЯ.LitRPG\Hеофициальная серия книг (разные издательства)\
  Поселягин-Владимир\Криминал\N. Title.fb2` — подтверждает, что именно
  структура "СЕРИЯ.LitRPG\...\Автор-Фамилия\Серия\" (вложенная
  папка-автор → папка-серия) — триггер.
- **Игнатов Михаил\Путь** (8 записей) — тот же паттерн, но уже в ДРУГОМ
  верхнеуровневом сборнике ("Серия - «Боевая фантастика»(ЛенИздат)"),
  подтверждает, что баг не привязан к одной конкретной папке верхнего
  уровня, а к самой вложенной "Автор\Серия" структуре где угодно.

**Live-подтверждение (2026-09-02, машина с реальным доступом к
`\\turnkey\Docs\Books`) — исходный фикс НЕ останавливал утечку, найден
и починен настоящий источник.**

Собрал два репро на реальных файлах:
1. Узкий (10 файлов: `...\Поселягин-Владимир\Криминал\` + 5 файлов той
   же серии в `Серия - «Боевая фантастика»(ЛенИздат)\`) — баг НЕ
   воспроизвёлся ни с фиксом, ни без него. Значит триггеру нужен более
   широкий контекст автора.
2. Полный (все 293 реальных файла автора "Поселягин Владимир" по всей
   библиотеке + вся папка `Серия - «Боевая фантастика»(ЛенИздат)` as-is,
   727 файлов/232 автора без фильтрации — иначе multi-author guard в
   `pass2_series_filename.py` не даёт репрезентативной картины) — баг
   воспроизвёлся **уже с применённым фиксом `apply_series_consensus()`**:
   те же 11 протёкших записей (Криминал, Гаврош, Зург, Освобожденный,
   Ремонтник, Собиратель, Сопротивленец), что и в исходной production CSV.

Трассировкой (временные debug-print в `series_processor.py`,
`pass4_consensus.py`, `pass2_series_filename.py`) установлено: ни
`apply_series_consensus()`, ни его дублирующая логика в
`pass4_consensus.py` (~1093-1096, тоже пропатчена в этой сессии для
подстраховки) реально НЕ участвуют в этой конкретной утечке. Настоящий
источник — ТРЕТИЙ, независимый механизм: блок **"SERIES SUFFIX
CORRECTION"** в `Pass4Consensus.execute()` (`pass4_consensus.py`,
~строка 1144), который существует для другого легитимного случая
("Я - Миха 1." → "Миха" ⊂ "Я - Миха", восстановить обрезанный префикс).

Причина: на момент, когда этот блок строит `full_series` (все
`proposed_series` с "надёжным" источником по автору), `proposed_series`
folder_dataset-записи из вложенной "Автор\Серия" структуры
(`...\Поселягин-Владимир\Криминал\1. Дон.fb2`) **ещё содержит сырой**
`"Поселягин-Владимир\Криминал"` — POST-CHECK в `regen_csv.py`
(строка ~1159), который его отрезает, выполняется ПОЗЖЕ, уже ПОСЛЕ
Pass 4. SUFFIX CORRECTION видит короткое чистое "Криминал" (у соседнего
filename-файла) как суффикс длинного грязного варианта и "исправляет"
чистую запись до грязной — ровно наоборот тому, что должно произойти.

Фикс: в построении `full_series` исключаем любые значения с `\` —
дешёвая, безопасная защита независимо от того, ПОЧЕМУ у upstream-записи
временно грязное значение.

Закреплено тестом на точном срезе реальных данных (не синтетика "из
головы", а буквально записанное состояние двух реальных записей на
момент входа в `Pass4Consensus.execute()`):
`tests/integration/fb2parser_core/test_author_series_consensus.py::
TestSeriesSuffixCorrectionFolderPathLeak`. Полный regression-набор
(18 тестов) зелёный.

**Игнатов Михаил\Путь (8 записей) — тот же класс бага, не проверено
отдельно** (не было времени скопировать ещё 30 авторов), но механизм
общий и не специфичен для Поселягина — высокая уверенность, что фикс
закрывает все 88 записей, а не только эту одну серию.

**Баг №4 и №5 (найдены, НЕ починены — требуют доступа к реальным
файлам для трассировки, не воспроизводятся синтетически):** та же
"протечка имени автора в корень серии", что и баг №3, но по ДВУМ
другим, ещё не найденным механизмам:

- **№4 — `folder_hierarchy` не распознаёт папку-автора как папку-автора.**
  Файл `Серия - «Золотой век английского детектива»\Ле Карре\Джордж
  Смайли\Ле Карре. 1. Звонок мертвецу. 2. Убийство по-джентельменски.fb2`
  → `proposed_series = "Ле Карре\Джордж Смайли"`,
  `series_source = "folder_dataset+subfolder_hierarchy"`. Здесь корень
  "Ле Карре" — не место протечки extracted_series_candidate, а
  буквально имя папки автора ("Ле Карре"), которое постчек
  "Родитель\Подсерия" в `regen_csv.py` (~строка 1490,
  `_postcheck_build_hierarchical_series` или аналог) взял как есть, не
  распознав, что это папка-автор, а не папка-серия. Существующая
  защита "Cleaning up folder_hierarchy series with embedded author
  names" в `pass4_consensus.py` (~строка 816) почему-то не сработала
  для этого конкретного случая — не разобрано, почему.
- **№5 — голый инициал автора становится "корнем" серии.**
  Файлы `Серия - «Исторический детектив»\Сэнсом К. Дж. - Шардлейк 1.
  Горбун лорда Кромвеля.fb2` (и том 2) → `proposed_series = "Дж.\
  Шардлейк"`; `Серия - «Триллер & Детектив»\Камин. Классические
  детективы (Дом историй)\Битон М. С. - Хэмиш Макбет 1/2/3...fb2` →
  `proposed_series = "С.\Хэмиш Макбет"`. Оба `series_source =
  "filename_named_arc"`. ЭТИ файлы — плоские, без вложенных папок
  (в отличие от бага №4), значит источник протечки — чисто
  filename-парсинг (скорее всего разбор "Author И. О. - Series N.
  Title" неверно расщепляет составные инициалы автора "К. Дж." на
  "К."/"Дж." где-то в цепочке пассов, и последний фрагмент "Дж."
  случайно используется как root для иерархии). `_detect_named_arcs`
  (pass2_series_filename.py:1542) была прочитана и НЕ выглядит прямым
  источником (её паттерн даёт Root=реальная серия, Arc=подзаголовок —
  здесь всё наоборот), так что нужен дальнейший поиск в другом месте
  (вероятно, в логике постчека для folder_dataset-файлов с
  многосоставными инициалами автора в имени).

**Инструмент для следующей станции:** искать похожие случаи через
`proposed_series` с `\`, где "голова" (часть до `\`) совпадает (полностью
или как подстрока после нормализации) с `proposed_author` — см.
`heur4b.py`-подобный скрипт (не сохранён в репозитории, воспроизводится
по образцу из bug №3 выше). На момент находки — **83 таких записи**
всего по библиотеке (77 через author-consensus = баг №3, починен; 6
через folder_hierarchy/filename_named_arc = баги №4-5, ещё нет).

**Баг №6 (найден, НЕ починен) — папка-издательский-бренд трактуется
как книжная серия автора.** `Серия - «Абсолютное оружие»` — это папка
книжной СЕРИИ ИЗДАТЕЛЬСТВА (бренд, под которым разные авторы издают
СОВЕРШЕННО РАЗНЫЕ, не связанные между собой книги/циклы), но каждый
автор в ней получает `proposed_series = "Абсолютное оружие"`, хотя в
самом имени файла явно виден его РЕАЛЬНЫЙ цикл:
- `Бессонов А.- Миры Конфедерации 1. Змеи эскулапа.fb2` — это на самом
  деле том серии "Мир Алекса Королёва\Миры Конфедерации" (см. баг,
  уже чинившийся ранее в этой сессии для Бессонова), а не "Абсолютное
  оружие".
- `Головачев В.- Атлантарктида 1...fb2` и `...Запрещенная реальность
  9...fb2` — два РАЗНЫХ головачёвских цикла, оба помечены одним
  издательским брендом.

Похожая уже существующая защита ("Cleaning up folder_hierarchy series
with embedded author names" в `pass4_consensus.py`) на этот случай не
распространяется (это `folder_dataset`, не `folder_hierarchy`, и
проблема не в имени автора в серии, а в том, что папка = бренд
издательства, а не серия книги). Нужна отдельная эвристика: если папка
`Серия - «X»` содержит ≥N разных авторов И у большинства файлов в самом
имени виден СВОЙ явный номер+название цикла (паттерн `Автор.-
РеальнаяСерия N. Название`) — не использовать имя папки как
`proposed_series`, взять реальную серию из имени файла вместо этого.

Проверено: ≥4 разных авторов в одной "серии"-папке — 36 совпадений по
всей библиотеке, но большинство из них — ЛОЖНЫЕ срабатывания (общие
однословные названия вроде "Охотник"/"Целитель"/"Наемник", которые
разные авторы независимо использовали для СВОИХ никак не связанных
книг — это не баг, а совпадение названий). Нужна ручная проверка
каждого кандидата на реальной библиотеке — начать с "Абсолютное
оружие" (подтверждён) и, возможно, издательских папок с "Серия - «...»"
в названии (эвристика "папка называется 'Серия - «Бренд»'" точнее, чем
просто "много авторов").

**Баг №7 (найден, НЕ починен) — одна серия физически разбита на 2
группы компиляции без видимой причины.** Автор "Мигель де Сааведра
Сервантес", серия "М. Сервантес. Собрание сочинений в 5 томах" — ОДНА
и та же строка серии, ОДНА и та же папка
(`Сборник книг от Поручика\М. Сервантес. Собрание сочинений в 5
томах\`), но в CSV групп это ДВЕ РАЗНЫЕ строки: {1,2,3} и {4,5}. Не
разобрано, почему `find_groups()` не объединил их в одну группу —
возможно, различие в `series_complete`/`part_count`/ином вычисляемом
поле, из-за которого они не считаются "одной и той же" группой.
Нужна трассировка `fb2_compiler.find_groups()` на реальных файлах.

## Баг №8 — выбор "лучшей" предкомпиляции игнорировал реальную полноту содержимого — ✅ Починено

Найден при ручном разборе результата компиляции (скриншот пользователя,
группа "Земляной Андрей, Орлов Борис / Странник"): `compile_group()`
(`fb2_compiler.py:1000`) выбирала "лучшую" предкомпиляцию **только по
ширине номинального диапазона томов** (`max(precompiled, key=lambda t:
t[2]-t[1])`), совершенно не учитывая реальный размер файла/полноту
текста.

Реальный случай: "Проект «Оборотень» (Странник. Пенталогия) - 2010.fb2"
(номинально тома 1-5, 4.3 МБ) выбиралась поверх "Странник 1-3 - 2021"
(3.7 МБ) + "Академик (Странник 4-5) - 2022" (3.8 МБ) — вместе 7.5 МБ,
почти вдвое больше — потому что 2010-я редакция была урезанной/сокращённой
(проверено: раздел "Проект «Оборотень»" внутри неё ~73 КБ против ~315 КБ
в переиздании 2021, т.е. более чем в 4 раза короче). Полные переиздания
помечались `delete`, урезанное — `keep`: реальная потеря контента при
компиляции.

Фикс: если другие предкомпиляции ТОЧНО (без пропусков/наложений)
замощают тот же номинальный диапазон, что и "лучшая" по ширине, и
суммарно заметно (порог `best_size < tiling_size * 0.7`) крупнее её —
считаем "лучшую" подозрительно неполной, исключаем и пересчитываем
среди оставшихся.

Проверено на полной библиотеке Test1 (изолированно от несвязанной
локальной правки `app_settings.json` — стеш только `fb2_compiler.py`):
только 2 группы изменились, обе — подтверждённые реальные случаи:
"Странник" (целевой) и "Романов Герман / Маршал" ("Маршал 1-9 (без
иллюстраций)" уступил "Маршал 1-5" + "Маршал 6-9" — само название файла
подтверждает, что это урезанная версия). Закреплено тестами:
`tests/integration/fb2parser_core/test_compile_completeness.py` +
синтетическая (не из реальной библиотеки) fixture
`tests/data/compile_completeness/` — размер файлов там специально
сконструирован, а не взят из реальных книг.

## Баг №9 — превью имени файла в веб-UI расходилось с реальной сборкой — ✅ Починено

Найден при разборе реальной группы "Злотников Роман, Николаев Андрей /
Мир вечного" (2 арки, каждая — уже готовая тетралогия из 4 книг):
веб-страница со списком групп до компиляции показывала "Мир вечного
(Дилогия)", а реально скомпилированный файл назывался "Мир вечного
(Дилогия в 8 книгах)". Причина: `fb2parser_web/views.py`
(`_serialize_compiler_group`) содержал СВОЮ, отдельную и урезанную копию
формулы расчёта суффикса имени вместо вызова той же логики, что
`fb2_compiler.py:compile_group()` — копия не знала про arc-point
предкомпиляции/сервисные слова ("Тетралогия" и т.п.) и подстановку
реального суммарного числа книг.

Фикс: логика расчёта суффикса вынесена из `compile_group()` в отдельный
переиспользуемый метод `FB2CompilerService.compute_group_suffix()`;
и реальная сборка, и веб-превью теперь вызывают ЕГО — физически не
могут разойтись снова. Проверено на полной библиотеке Test1 (60 групп
через `_serialize_compiler_group`, 0 ошибок) и тестом
`test_web_preview_name_matches_real_compiled_name`.

## Баг №10 — `_run_stats()` путал дубль/опечатку номера тома с настоящей подсерией — ✅ Починено

Найден пользователем через реальную группу "Лисина Александра / Артур
Рэйш" (5 файлов: диапазоны 1-4, 5-6, 7-8 + два РАЗНЫХ файла на одной и
той же позиции 9 — "09. Последний бог.fb2" и "09.Проклятие
королей.fb2", опечатка в номере у одного из них): компилятор показывал
"Тетралогия в 10 книгах" — внутренне противоречивое число (не совпадает
ни с числом файлов, ни с реальным диапазоном 1-9).

Этот же баг ранее уже пытались чинить на другой станции (`fc636a8`), но
коммит отревертнули (`24afedf`) без объяснения причины — оказалось,
частичный фикс поправлял только подсчёт n_volumes, но не признак
`has_subseries` (условие `len(_top_pos_list) > len(set(_top_pos_list))`
ошибочно трактовало ЛЮБОЕ повторение верхней позиции как "несколько
дуг", включая банальный дубль), из-за чего слово "Тетралогия" осталось
бы неверным даже с починенным числом книг.

Полный фикс (обе части):
1. `has_subseries` теперь требует, чтобы у повторяющейся top-позиции
   были РАЗНЫЕ под-идентичности `(sort_key[2], sort_key[3])` — не просто
   разные файлы на одной позиции.
2. Подсчёт `n_volumes` сворачивает одиночные позиции по полному ключу
   `(sort_key[1], sort_key[2], sort_key[3])`, а не только по верхней
   позиции — иначе легитимные разные под-книги одной дуги (например
   "5.1"/"5.2"/"5.3"/"5.4" через `sort_key[2]`, или "Часть 1"/"Часть 2"
   через `sort_key[3]` при одинаковом `sort_key[2]`) ошибочно
   схлопывались бы в одну книгу — это была РЕАЛЬНАЯ регрессия в первой
   версии этого фикса на моей собственной сессии (случай "Иванович
   Юрий / Миры Доставки", 4 книги → ошибочно 1), пойманная только
   благодаря полному библиотечному diff перед коммитом.

Проверено на полной библиотеке Test1 (изолированно от несвязанной
правки `app_settings.json`): осталось ровно 2 отличия от baseline, оба
подтверждены как исправления, не регрессии — "Артур Рэйш" (целевой) и
"Мур Кристофер / Хвойная Бухта" (тот же класс бага на под-уровне: два
РАЗНЫХ файла случайно с одинаковой под-позицией "3.2" → корректно не
задваивались, 4→3). 0 ошибок. Закреплено тестами:
`tests/integration/fb2parser_core/test_run_stats_duplicate_position.py`
(3 сценария: дубль-позиция, подсерия по sort_key[2], подсерия по
sort_key[3] — все три должны различаться корректно).

## Баг №11 — самоподтверждающаяся metadata_series ложно сливала несвязанные серии в `find_groups()` — ✅ Починено

Найден пользователем через реальную группу "Лисина Александра": серия
"Времена" (10 книг, полностью независимая) исчезала из списка — вместо
неё показывалась "Тёмные времена" из 10 книг, хотя реальная "Тёмные
времена" содержит всего 3 книги ("01 Враг.fb2", "02 Попутчик.fb2", "03
Хозяин.fb2"). При синхронизации с `delete_sources=True` эти 3 реальные
книги были бы удалены как "дубликаты".

Причина — в комбинированном слиянии бакетов `find_groups()`: короткая
серия "времена" текстово является хвостовым суффиксом длинной серии
"темные времена" (`_word_suffix_key`), и по правилам слияния требуется
metadata-подтверждение — хотя бы одна запись из бакета ДЛИННОЙ серии
должна иметь `metadata_series`, совпадающий с короткой. Но проверка не
исключала тривиальный случай: одна из книг папки «Тёмные времена» имела
`metadata_series="Темные времена"` — то есть ПОЛНОСТЬЮ совпадающий с
именем ЕЁ ЖЕ собственного (длинного) бакета, а не с короткой серией.
Поскольку "времена" — последнее слово в "темные времена",
`_word_suffix_key("времена", "темные времена")` возвращал True, и такое
самоподтверждение (metadata == собственный длинный бакет) ошибочно
принималось как доказательство связи с СОВСЕМ другой короткой серией.

Фикс: исключаем из подтверждения `metadata_series`, полностью
совпадающий с каноническим (длинным) ключом бакета — такое значение
подтверждает только "эта книга принадлежит своей же папке", а не связь
с другой, более короткой серией.

Проверено на полной библиотеке Test1 (1676 групп, изолированно от
несвязанных правок `app_settings.json`/`genres.xml` через `git stash
push` на конкретный файл): ровно 1 отличие от baseline — "Времена"
(10 книг) и "Тёмные времена" (3 книги) корректно разделились на две
группы вместо одной ложно склеенной "Тёмные времена" (10 книг,
содержащей чужие файлы). Полный regression-suite (20→21 тест) не
затронут. Закреплено тестом:
`tests/integration/fb2parser_core/test_bucket_merge_self_referential_metadata.py`.

## Баг №12 — sync-time дедупликация фрагментировала компиляции и теряла числовой диапазон из имени файла — ✅ Починено

Найден пользователем через реальную группу "Лисина Александра / Артур
Рэйш": пре-компиляция (превью, `find_groups`/`compile_group`) верно
показывала "Ноналогия" (9 томов), но после реальной синхронизации
итоговый скомпилированный файл получал "Октология" (8) — числа не
совпадали.

Причина — совсем другой, более ранний и наивный шаг обработки:
`SynchronizationService._deduplicate_by_compilation()` +
`_build_target_filename()` (`synchronization.py`), который работает ДО
реального движка компиляции (`find_groups`/`compile_group`/`_run_stats`
в `fb2_compiler.py`) и понятия не имеет о полной картине серии. На
реальных файлах Рэйша (диапазоны 1-2, 3-4, 5-6, 7-8 + уже готовый
"Артур Рэйш (Сборник, кн. 1-4)", покрывающий 1-4) этот шаг:

1. Удалял только ОДИНОЧНЫЕ тома, полностью покрытые компиляцией — но
   НЕ удалял избыточные compilation-файлы друг относительно друга.
   Из-за этого "1-2" и "3-4" переезжали в библиотеку КАК ОТДЕЛЬНЫЕ
   файлы РЯДОМ с уже готовым "1-4" — контент томов 1-4 физически
   дублировался (существовал дважды).
2. Каждый выживший compilation-файл переименовывался НЕЗАВИСИМО, суффиксом
   посчитанным только по ЕГО СОБСТВЕННОМУ локальному диапазону
   (`_series_suffix(len(covered_volumes), lo, hi)` без знания об остальных
   диапазонах серии) — "1-2"→"Дилогия", "3-4"→"т. 3-4" и т.п. — вместо
   одной книги получался набор внутренне противоречивых файлов. Хуже:
   такое переименование НЕОБРАТИМО стирало из имени файла числовой
   диапазон (в "Дилогия" числа уже нет) — из-за чего последующий
   auto-compile, сканируя эти уже переименованные файлы в библиотеке, не
   мог восстановить их истинные позиции и в итоге считал диапазон серии
   неверно ("Октология" вместо "Ноналогия").

Фикс (в два этапа, второй — сужение первого после находки риска):

1. `_deduplicate_by_compilation()`: жадно (от большего диапазона к
   меньшему) дедуплицирует compilation-записи такой же связки
   (автор, серия) друг относительно друга — не только одиночки против
   компиляций. `_build_target_filename()` для compilation-записей больше
   не строит суффикс по локальному диапазону — сохраняет ОРИГИНАЛЬНОЕ имя
   файла (числовой диапазон остаётся читаемым для последующего
   auto-compile; финальный суффикс серии строит именно он, у него есть
   полная картина).
2. Первая версия фикса на полной библиотеке Test1 дала слишком широкий
   эффект — 120 новых удалений вместо ожидаемых 2-4 для целевого случая.
   Разбор показал реальный риск: часть "покрытий" определялась не по
   явному числу, а по УГАДАННОМУ ключевому слову (`_COMPILATION_KEYWORDS`,
   напр. "трилогия"→{1,2,3}) — у серии "Волжане"
   (Архипов Андрей) нашлось 3 РАЗНЫХ физических файла ("Поветлужье
   (трилогия)", "Волжане (Волжане. Трилогия)", "Цикл «Волжане»"), все
   классифицируются как покрывающие {1,2,3} — но совпадение УГАДАННОГО
   диапазона не доказывает идентичность контента (в отличие от явного
   числового диапазона, как у Рэйша). `_classify_record()` теперь
   возвращает флаг `confident` (True только для явного числа —
   `series_number`-диапазон, "(N-M)" в имени/заголовке, "Том N"; False —
   для угаданного по слову). Новая жадная дедупликация участвует ТОЛЬКО
   между confident-компиляциями; неуверенные ("трилогия" и т.п.) никогда
   не удаляются и никогда не служат основанием для удаления других — та
   же философия "безопаснее ничего не делать", что уже была у веток
   одиночных томов в этой функции.

Проверено на полной библиотеке Test1 (изолированно от несвязанных правок
через `git stash push` на конкретный файл): после сужения diff от
baseline составил 75 новых удалений (было 120 до сужения) — все
проверенные образцы оказались настоящими дубликатами с явным числовым
диапазоном (напр. "Оружейники 1-3"/"4-6" существуют БУКВАЛЬНО в двух
разных папках библиотеки как точные копии; "Ван Ван из Чайны 1-4"
полностью покрыт более полным переизданием "1-5"), рискованные
guessed-диапазон случаи ("Волжане" и подобные) из diff'а пропали. Полный
regression-suite (21→24 теста) не затронут. Закреплено тестами:
`tests/integration/fb2parser_core/test_sync_compilation_dedup.py`.

## Баг №13 — однословная серия перед "Книги N-M" терялась, а затем ложно переклеивалась на чужую серию по совпадению последнего слова — ✅ Починено

Найден пользователем через ту же реальную группу "Лисина Александра":
после фикса бага №11 логика компиляции всё ещё не видела, что уже
готовый файл всей серии "Времена" (папка "Сборники",
"Времена. Книги 1-10.fb2", 10 томов) — то же самое, что 10 отдельных
файлов серии "Времена". Хуже: при повторной проверке итоговое
скомпилированное имя оказалось "Тёмные времена" — совсем другой, но
СОСЕДНЕЙ серии того же автора (3 отдельные книги).

Причина — двойная, оба бага в `regen_csv`-пайплайне определения серии
(отдельно от `fb2_compiler.py`, где уже чинили похожий класс багов —
№9, №11):

1. `Pass2SeriesFilename._extract_series_from_filename()`, Правило 3:
   однословный кандидат серии перед точкой ("Времена. Книги 1-10")
   безусловно трактовался как фамилия автора (`_is_author_pattern`,
   `len(_ps_words) <= 1`), после чего Правило 3B извлекало в качестве
   "серии" уже само служебное слово-обёртку после точки ("Книги") —
   `proposed_series` оставалась пустой, файл выпадал из группировки по
   серии целиком (не участвовал ни в `find_groups()`, ни в sync-time
   дедупликации).
2. После починки (1) файл получал корректное `proposed_series="Времена"`
   (`series_source="filename"`) — но затем "SERIES SUFFIX CORRECTION" в
   `Pass4Consensus.execute()` "исправляла" его на "Тёмные времена":
   правило ищет случаи вида "Я - Миха" (полное имя серии, подтверждённое
   у другого файла автора) + "Миха" (обрезанный filename-суффикс той же
   книги) и достраивает префикс. Проверка была построена ТОЛЬКО на
   текстовом совпадении хвостового слова (`full.endswith(cur)`) — а
   "времена" оказывается последним словом в "тёмные времена" совершенно
   случайно, у другого файла того же автора («01 Враг.fb2» и т.п.,
   folder_dataset, самостоятельная серия). Итог: 3 реальные книги
   "Тёмные времена" оказывались бы "покрыты" 10-томным файлом и удалены
   при синхронизации — тот же класс риска, что и в баге №11, но в
   независимом коде.

Фикс:
1. Правило 3 теперь распознаёт, что текст ПОСЛЕ первой точки — не
   заголовок книги, а служебная обёртка сборника/тома (`collection_keywords`
   из настроек, либо голое "книги|книга|тома|том|части|часть N") — в этом
   случае однословный кандидат ПЕРЕД точкой возвращается как есть, минуя
   и author-эвристику, и заход в Правило 3B.
2. SERIES SUFFIX CORRECTION теперь требует, чтобы ПОЛНОЕ имя "покрывающей"
   серии реально встречалось в имени ФАЙЛА текущей записи (как в
   исходном примере "Я - Миха 1. Дикарь" — там "я - миха" целиком есть в
   стеме) — простое совпадение хвостового слова этого не доказывает и
   больше не принимается как основание для замены.

Проверено на полной библиотеке Test1 (12231 запись, изолированно от
несвязанных правок): ровно 5 отличий от baseline — целевой случай
"Времена" (теперь верно распознаётся find_groups()/sync-дедупликацией
как эквивалент 10 файлам), одно явное улучшение ("Уроборос. Том 1.fb2" —
одиночная книга раньше вообще не получала серию, теперь получает), одна
косметическая смена источника без изменения самого значения серии
("Чекисты рассказывают"), и один пограничный случай (2 из 6 файлов
Сэнсома "Шардлейк"/"Мэтью Шардлейк" — переход на "Шардлейк", который уже
был подтверждён `filename+meta_confirmed`-источником у большинства (4 из
6) файлов той же серии; не риск потери данных, только выбор варианта
имени). Полный regression-suite (24→28 тестов) не затронут. Закреплено
тестами:
`tests/integration/fb2parser_core/test_series_wrapper_and_suffix_guard.py`.

## Баг №14 — единственный выживший после дедупликации compilation-файл никогда не переименовывался — ✅ Починено

Найден пользователем при живой проверке реальной синхронизации (после
фиксов багов №12/№13): в библиотеке файл серии "Времена" остался под
СЫРЫМ исходным именем "Времена. Книги 1-10.fb2" — без автора и суффикса
серии — хотя ВСЕ остальные серии автора получили правильное имя вида
"Лисина Александра - Название (Суффикс).fb2".

Причина: дедупликация в баге №12 теперь ПРАВИЛЬНО удаляет все 10
одиночных томов "Времена" как покрытые уже готовым файлом "Времена.
Книги 1-10.fb2" — но в результате этот файл остаётся ЕДИНСТВЕННЫМ
выжившим для своей (автор, серия) связки. `_build_target_filename()`
(баг №12) намеренно сохраняет для compilation-записей ОРИГИНАЛЬНОЕ имя
файла, откладывая построение финального суффикса на более поздний шаг —
`auto_compile_library()` через `find_groups()`, у которого есть полная
картина всех томов серии. Но `find_groups()` требует МИНИМУМ 2 файла,
чтобы вообще считать это "группой" (`if len(recs) < 2: continue`) — если
дедупликация схлопнула серию до 1 файла, эта группа никогда не создаётся
и никогда не переименовывается.

Фикс: сразу после дедупликации в `synchronize()` вычисляем, для каких
(автор, серия) связок остался РОВНО один compilation-файл с известным
диапазоном, начинающимся с 1 (`_sole_full_compilation_paths`) — для них
`_build_target_filename()` строит финальное имя ("Автор - Серия
(Суффикс).fb2") сразу, а не откладывает на auto-compile: раз файл один
и покрывает диапазон с первого тома, это уже вся серия целиком, полная
картина уже известна на этом шаге.

Проверено синтетическим воспроизведением реального случая (10 одиночных
томов + готовый файл "1-10", дедупликация оставляет 1 файл → получает
имя "Лисина Александра - Времена (Декалогия).fb2"). Полный
regression-suite (28→29 тестов) не затронут. Закреплено тестом:
`tests/integration/fb2parser_core/test_sync_compilation_dedup.py`
(`TestSoleSurvivingCompilationGetsProperName`).
